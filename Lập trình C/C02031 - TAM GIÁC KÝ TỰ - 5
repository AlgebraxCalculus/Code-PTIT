#include <stdio.h>

int main() {
	int n;
	scanf("%d", &n);
	for (int i = n; i >= 1; i--) {
		int k = i;
		for (int j = 1; j <= i; j++) {
			printf("%c", k + 63);
			k++;
		}
		printf("\n");
	}
	return 0;
}
