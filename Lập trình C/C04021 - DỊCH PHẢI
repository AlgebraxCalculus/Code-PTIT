#include <stdio.h>

int main() {
	int n;
	scanf("%d", &n);
	int a[n + 5];
	for (int i = 0; i < n; i++) {
		scanf("%d", &a[i]);
	}
	int vitri;
	scanf("%d", &vitri);
	for (int i = n - vitri; i < n; i++) {
		printf("%d ", a[i]);
	}
	for (int i = 0; i < n - vitri; i++) {
		printf("%d ", a[i]);
	}
	return 0;
}
