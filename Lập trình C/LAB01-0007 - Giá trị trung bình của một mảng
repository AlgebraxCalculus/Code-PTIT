#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    int a[n + 5];
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }
    int tong = 0;
    for (int i = 0; i < n; i++) {
        tong += a[i];
    }
    printf("%0.3f", tong * 1.0 / n);
    return 0;
}
