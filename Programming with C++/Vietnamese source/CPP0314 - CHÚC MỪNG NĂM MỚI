#include <bits/stdc++.h>

using namespace std;

int main() {
	int n;
	cin >> n;
	set<string> se;
	for (int i = 0; i < n; i++) {
		string s;
		scanf("\n");
		getline(cin, s);
		se.insert(s);
	}
	cout << se.size();
	return 0;
}
