#include <iostream>
#include <algorithm>

using namespace std;

int main() {
	int t;
	cin >> t;
	while (t--) {
		long long a, x, y;
		cin >> a >> x >> y;
		for (int i = 1; i <= __gcd(x, y); i++) {
			cout << a;
		}
		cout << endl;
	}
	return 0;
}
