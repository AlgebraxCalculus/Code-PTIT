#include <iostream>
#include <math.h>

using namespace std;

int main() {
	int a, b, sum = 0;
	cin >> a >> b;
	int x = sqrt(a), y = sqrt(b);
	if (x * x != a) {
		x++;
	}
	for (int i = x; i <= y; i++) {
		sum += (i * i);
	}
	cout << sum;
	return 0;
}
