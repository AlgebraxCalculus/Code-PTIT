#include <bits/stdc++.h>
#include <set>

using namespace std;

int main() {
	string s;
	cin >> s;
	set<char> se;
	for (int i = 0; i < s.length(); i++) {
		se.insert(s[i]);
	}
	cout << se.size();
	return 0;
}
